[package]
name = "mimi"
version = "0.1.0"
authors = ["Ash <relativistic.policeman@gmail.com>"]
edition = "2018"

[dependencies]
maplit = "^1.0"
pest = "^2.1"
pest_derive = "^2.1"
termion = "^1.5"

# Only needed for the example binaries.
failure = "^0.1"
structopt = "0.2"

tui = "0.3"

[[bin]]
name = "demo"
path = "src/bin/demo.rs"
